# API Reference

Full API documentation auto-generated from source code docstrings.

## Package Structure

The `alt_nfp` package is organised into:

- **Core modules** — model, sampling, data loading, forecasting.
- **`lookups`** — static reference tables (industry, geography, schedules).
- **`ingest`** — raw data ingestion and observation panel construction.
- **`vintages`** — revision tracking, download, and processing pipeline.

Browse the sub-packages below or use the search bar to find specific
functions and classes.
